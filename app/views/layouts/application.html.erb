<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
  <title><%= "#{current_account.subdomain} | " unless current_account.nil? %>Twik</title>
  <%= favicon_link_tag 'twik-robot-red.ico' %>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <nav class="navbar navbar-default">
      <div class="container-full">
        <div class="col-md-12">
          <div class="navbar-header">
            <%= link_to root_path, class: "navbar-brand twik" do %>
              <%= image_tag "twik-logo-word-only-red.png", class: "img-responsive" %>
            <% end %>
          </div>
          <% unless current_account.nil? %>
            <div class="current-account">
              <p class="navbar-text"><%= current_account.subdomain %>.twik.us</a></p>
            </div>
          <% end %>
        </div>
      </div>
    </nav>
    <nav class="navbar navigation-actions">
      <ul class="nav navbar-nav action-navbar">
        <% if user_signed_in? %>
          <li><%= link_to "Create Menu", new_menu_path %> <span class="nav-separator">|</span></li>
          <li><%= link_to "Manage Team", users_path %> <span class="nav-separator">|</span></li>
          <li><%= link_to "Search", '#' %> <span class="nav-separator">|</span></li>
          <li><%= link_to "Log out", destroy_user_session_path, method: 'delete', class: "" %></li>
        <% else %>
          <li><%= link_to "About Us", new_menu_path %> <span class="nav-separator">|</span></li>
          <li><%= link_to "Pricing", users_path %> <span class="nav-separator">|</span></li>
          <li><%= link_to "Log In", '#' %></li>
        <% end %>
      </ul>
    </nav>
  </header>
  <section class="container" id="main-container">
    <%= flash_messages if show_layout_flash? %>
    <%= yield %>
  </section>
  <% if user_signed_in? %>
    <footer id="main-footer" class="layout-footer container">
      <hr>
      <h1>&copy; Twik, 2016</h1>
    </footer>
  <% end %>
  <div id="loader-wrapper" class="hide">
    <div id="loader">
      <div id="top"></div>
      <div id="bottom"></div>
      <div id="line"></div>
    </div>
  </div>
  <%= javascript_include_tag 'application' %>
</body>
</html>
